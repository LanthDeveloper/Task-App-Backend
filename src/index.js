//src/index.js

import app from "./app.js";
import { SERVER_PORT, SERVER_HOST } from "./vars/vServer.js";

// Start the server
/* app.listen(SERVER_PORT, () => {
  console.log(`✅ Backend running on: http://${SERVER_HOST}:${SERVER_PORT}`);
}); */
